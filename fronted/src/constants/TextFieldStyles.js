/**
 * Enum to indicate the which styles will be used in the TextField component
 * @enum {Object} 
 * @readonly
 */
export const TextFieldStyles = Object.freeze({
   FILLED: Symbol("filled"),
   OUTLINE: Symbol("outline"),
});